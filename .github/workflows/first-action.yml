name: first workflow
on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: get code
        uses: actions/checkout@v3 
      - name: SSH to Runner session
        uses: mxschmitt/action-tmate@v3
        with:
          ssh-only: true
      - name: Wait for user connection
        run: sleep 2m    
    
